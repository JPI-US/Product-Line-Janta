/** Hero tower framing — load pose matches Janta Vision reference */
export const HERO_TOWER_POSE = {
  initialRotationY: 0.62,
  cameraPosition: [6.5, 4.2, 9.5] as const,
  cameraTarget: [0, 1, 0] as const,
  cameraFov: 35,
} as const;
